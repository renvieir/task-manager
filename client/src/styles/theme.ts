import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      light: "#ffffff",
      main: "#e0e0e0",
      dark: "#aeaeae",
    },
    secondary: {
      light: "#c1d5e0",
      main: "#90a4ae",
      dark: "#62757f",
    },
  },
});

export default theme;
